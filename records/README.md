# Records

This folder stores run-specific artifacts and notebooks that document completed proofs or submission evidence.

- Keep the top-level notebooks in the repo as clean templates/runbooks.
- Save concrete completed runs here so the reproducible template and the actual evidence do not overwrite each other.
